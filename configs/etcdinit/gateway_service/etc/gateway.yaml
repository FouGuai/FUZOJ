Name: gateway
Host: 0.0.0.0
Port: 0
Timeout: 0
bootstrap:
  etcd:
    hosts:
      - 127.0.0.1:2379
    key: bootstrap
  keys:
    config: gateway.config
    runtime: gateway.rest.runtime
    log: gateway.log
Middlewares:
  Trace: false
  Log: false
  Prometheus: false
  MaxConns: false
  Breaker: false
  Shedding: false
  Timeout: false
  Recover: true
  Metrics: false
  MaxBytes: false
  Gunzip: false

logger:
  level: "info"
  format: "json"
  outputPath: "stdout"
  errorPath: "stderr"
  service: "gateway"
  env: "dev"
  cluster: "default"

auth:
  jwtSecret: "tyylovezsrtyylovezsr"
  jwtIssuer: "fuzoj"

redis:
  host: "127.0.0.1:6379"
  type: "node"

kafka:
  brokers: ["127.0.0.1:9092"]
  conns: 1
  consumers: 2
  processors: 2

banEvent:
  enabled: true
  topic: "user.events"
  consumerGroup: "fuzoj-gateway-bans"

cache:
  banLocalTTL: 30m
  banLocalSize: 100000
  tokenBlacklistCacheTTL: 2m

rateLimit:
  window: 1m
  userMax: 200
  ipMax: 500
  routeMax: 1000

proxy:
  maxIdleConns: 2048
  maxIdleConnsPerHost: 256
  idleConnTimeout: 90s
  responseHeaderTimeout: 5s
  tlsHandshakeTimeout: 5s
  dialTimeout: 3s

cors:
  enabled: true
  allowedOrigins: ["*"]
  allowedMethods: ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"]
  allowedHeaders: ["Authorization", "Content-Type", "Idempotency-Key", "X-Trace-Id"]
  exposedHeaders: ["X-Trace-Id", "X-Request-Id"]
  allowCredentials: false
  maxAge: 12h

upstreams:
  - name: "user"
    http:
      target: "127.0.0.1:8081"
      timeout: 0
    mappings:
      - name: "user.register"
        method: "POST"
        path: "/api/v1/user/register"
        auth:
          mode: "public"
      - name: "user.login"
        method: "POST"
        path: "/api/v1/user/login"
        auth:
          mode: "public"
      - name: "user.refresh"
        method: "POST"
        path: "/api/v1/user/refresh-token"
        auth:
          mode: "public"
      - name: "user.logout"
        method: "POST"
        path: "/api/v1/user/logout"
        auth:
          mode: "protected"
          roles: ["user", "problem_setter", "admin", "super_admin"]

  - name: "problem"
    http:
      target: "127.0.0.1:8083"
      timeout: 0
    mappings:
      - name: "problem.public.latest"
        method: "GET"
        path: "/api/v1/problems/:id/latest"
        auth:
          mode: "public"
      - name: "problem.public.statement"
        method: "GET"
        path: "/api/v1/problems/:id/statement"
        auth:
          mode: "public"
      - name: "problem.public.statement-version"
        method: "GET"
        path: "/api/v1/problems/:id/versions/:version/statement"
        auth:
          mode: "public"
      - name: "problem.manage.create"
        method: "POST"
        path: "/api/v1/problems"
        auth:
          mode: "protected"
          roles: ["user", "problem_setter", "admin", "super_admin"]
      - name: "problem.manage.delete"
        method: "DELETE"
        path: "/api/v1/problems/:id"
        auth:
          mode: "protected"
          roles: ["user", "problem_setter", "admin", "super_admin"]
      - name: "problem.manage.statement-update"
        method: "PUT"
        path: "/api/v1/problems/:id/versions/:version/statement"
        auth:
          mode: "protected"
          roles: ["user", "problem_setter", "admin", "super_admin"]
      - name: "problem.manage.upload-prepare"
        method: "POST"
        path: "/api/v1/problems/:id/data-pack/uploads:prepare"
        auth:
          mode: "protected"
          roles: ["user", "problem_setter", "admin", "super_admin"]
      - name: "problem.manage.upload-sign"
        method: "POST"
        path: "/api/v1/problems/:id/data-pack/uploads/:upload_id/sign"
        auth:
          mode: "protected"
          roles: ["user", "problem_setter", "admin", "super_admin"]
      - name: "problem.manage.upload-complete"
        method: "POST"
        path: "/api/v1/problems/:id/data-pack/uploads/:upload_id/complete"
        auth:
          mode: "protected"
          roles: ["user", "problem_setter", "admin", "super_admin"]
      - name: "problem.manage.upload-abort"
        method: "POST"
        path: "/api/v1/problems/:id/data-pack/uploads/:upload_id/abort"
        auth:
          mode: "protected"
          roles: ["user", "problem_setter", "admin", "super_admin"]
      - name: "problem.manage.publish"
        method: "POST"
        path: "/api/v1/problems/:id/versions/:version/publish"
        auth:
          mode: "protected"
          roles: ["user", "problem_setter", "admin", "super_admin"]

  - name: "submit"
    http:
      target: "127.0.0.1:8086"
      timeout: 0
    mappings:
      - name: "submit.create"
        method: "POST"
        path: "/api/v1/submissions"
        auth:
          mode: "protected"
          roles: ["user", "problem_setter", "admin", "super_admin"]
      - name: "submit.status"
        method: "GET"
        path: "/api/v1/submissions/:id"
        auth:
          mode: "protected"
          roles: ["user", "problem_setter", "admin", "super_admin"]
      - name: "submit.source"
        method: "GET"
        path: "/api/v1/submissions/:id/source"
        auth:
          mode: "protected"
          roles: ["user", "problem_setter", "admin", "super_admin"]
      - name: "submit.batch"
        method: "POST"
        path: "/api/v1/submissions/batch_status"
        auth:
          mode: "protected"
          roles: ["user", "problem_setter", "admin", "super_admin"]

  - name: "status"
    http:
      target: "127.0.0.1:8089"
      timeout: 0
    mappings:
      - name: "status.get"
        method: "GET"
        path: "/api/v1/status/:resource/:id"
        auth:
          mode: "protected"
          roles: ["user", "problem_setter", "admin", "super_admin"]

  - name: "judge"
    http:
      target: "127.0.0.1:8085"
      timeout: 0
    mappings:
      - name: "judge.status"
        method: "GET"
        path: "/api/v1/judge/:resource/:id"
        auth:
          mode: "protected"
          roles: ["user", "problem_setter", "admin", "super_admin"]

  - name: "contest"
    http:
      target: "127.0.0.1:0"
      timeout: 0
    mappings:
      - name: "contest.public"
        method: "GET"
        path: "/api/v1/contests"
        auth:
          mode: "public"
      - name: "contest.public.detail"
        method: "GET"
        path: "/api/v1/contests/:id"
        auth:
          mode: "public"
      - name: "contest.public.action"
        method: "GET"
        path: "/api/v1/contests/:id/:action"
        auth:
          mode: "public"
      - name: "contest.manage"
        method: "POST"
        path: "/api/v1/contests"
        auth:
          mode: "protected"
          roles: ["user", "problem_setter", "admin", "super_admin"]
      - name: "contest.manage"
        method: "POST"
        path: "/api/v1/contests/:id"
        auth:
          mode: "protected"
          roles: ["user", "problem_setter", "admin", "super_admin"]
      - name: "contest.manage"
        method: "POST"
        path: "/api/v1/contests/:id/:action"
        auth:
          mode: "protected"
          roles: ["user", "problem_setter", "admin", "super_admin"]
      - name: "contest.manage"
        method: "PUT"
        path: "/api/v1/contests/:id"
        auth:
          mode: "protected"
          roles: ["user", "problem_setter", "admin", "super_admin"]
      - name: "contest.manage"
        method: "PUT"
        path: "/api/v1/contests/:id/:action"
        auth:
          mode: "protected"
          roles: ["user", "problem_setter", "admin", "super_admin"]
      - name: "contest.manage"
        method: "DELETE"
        path: "/api/v1/contests/:id"
        auth:
          mode: "protected"
          roles: ["user", "problem_setter", "admin", "super_admin"]
      - name: "contest.manage"
        method: "DELETE"
        path: "/api/v1/contests/:id/:action"
        auth:
          mode: "protected"
          roles: ["user", "problem_setter", "admin", "super_admin"]

  - name: "minio"
    http:
      target: "127.0.0.1:9000"
      timeout: 0
    mappings:
      - name: "objects.download"
        method: "GET"
        path: "/objects/*any"
        auth:
          mode: "public"

  - name: "rank"
    http:
      target: "127.0.0.1:8091"
      timeout: 0
    mappings:
      - name: "rank.leaderboard"
        method: "GET"
        path: "/api/v1/contests/:id/leaderboard"
        auth:
          mode: "public"

  - name: "rank-ws"
    http:
      target: "127.0.0.1:8092"
      timeout: 0
    mappings:
      - name: "rank.ws.leaderboard"
        method: "GET"
        path: "/api/v1/contests/:id/leaderboard/ws"
        auth:
          mode: "public"
